# Step 1: Scaffold & Dependencies

> Muc tieu: Tao project PPTB tool voi day du dependencies va config

---

## 1.1 Scaffold Project

```bash
cd src2/
npx --package yo --package generator-pptb -- yo pptb
```

Chon:
- Template: **React + TypeScript**
- Tool name: `xrm-quick-translate`

---

## 1.2 package.json

```json
{
  "name": "@phuocle/xrm-quick-translate",
  "version": "1.0.0",
  "displayName": "Xrm Quick Translate",
  "description": "Bulk translation management for Dataverse metadata",
  "main": "index.html",
  "icon": "icons/tool.svg",
  "license": "MIT",
  "contributors": [{ "name": "Phuoc Le" }],
  "configurations": {
    "repository": "https://github.com/phuocle/Xrm-Quick-Edit"
  },
  "features": {
    "minAPI": "1.0.20"
  },
  "cspExceptions": {
    "connect-src": [
      { "domain": "api.cognitive.microsofttranslator.com", "exceptionReason": "Azure Translator", "optional": true },
      { "domain": "api-free.deepl.com", "exceptionReason": "DeepL API Free", "optional": true },
      { "domain": "api.deepl.com", "exceptionReason": "DeepL API Pro", "optional": true },
      { "domain": "generativelanguage.googleapis.com", "exceptionReason": "Gemini AI", "optional": true },
      { "domain": "glosbe.com", "exceptionReason": "Glosbe translation", "optional": true }
    ]
  },
  "devDependencies": {
    "@pptb/types": "latest",
    "typescript": "^5.5.0",
    "vite": "^5.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "@fluentui/react-components": "^9.56.0",
    "@fluentui/react-icons": "^2.0.0",
    "ag-grid-react": "^32.0.0",
    "ag-grid-community": "^32.0.0"
  }
}
```

---

## 1.3 tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "src",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

---

## 1.4 vite.config.ts

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  },
  build: {
    outDir: 'dist',
    rollupOptions: {
      output: {
        entryFileNames: 'assets/[name].js',
        chunkFileNames: 'assets/[name].js',
        assetFileNames: 'assets/[name].[ext]'
      }
    }
  }
});
```

---

## 1.5 index.html

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Xrm Quick Translate</title>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/app.tsx"></script>
</body>
</html>
```

---

## 1.6 src/app.tsx (Placeholder)

```tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { FluentProvider, webLightTheme } from '@fluentui/react-components';

const App: React.FC = () => {
  return (
    <FluentProvider theme={webLightTheme}>
      <div>Xrm Quick Translate - Loading...</div>
    </FluentProvider>
  );
};

createRoot(document.getElementById('root')!).render(<App />);
```

---

## Acceptance Criteria

- [ ] `npm install` chay thanh cong, khong error
- [ ] `npm run dev` khoi dong Vite dev server
- [ ] Truy cap localhost thay duoc "Xrm Quick Translate - Loading..."
- [ ] `npx pptb-validate` pass (validate package.json format)
- [ ] TypeScript compile khong loi
